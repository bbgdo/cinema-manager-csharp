<Application x:Class="CinemaManager.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CinemaManager.Wpf.Converters"
             Startup="OnStartup">
    <Application.Resources>

        <SolidColorBrush x:Key="BgDeep" Color="#0C0C14"/>
        <SolidColorBrush x:Key="BgHeader" Color="#13131F"/>
        <SolidColorBrush x:Key="BgCard" Color="#1A1A2A"/>
        <SolidColorBrush x:Key="BgCardHov" Color="#22223A"/>
        <SolidColorBrush x:Key="BgInfoChip" Color="#1E2540"/>
        <SolidColorBrush x:Key="BgGenreChip" Color="#321E10"/>
        <SolidColorBrush x:Key="Accent" Color="#E63946"/>
        <SolidColorBrush x:Key="AccentAmber" Color="#F4A261"/>
        <SolidColorBrush x:Key="FgPrimary" Color="#EEEEF5"/>
        <SolidColorBrush x:Key="FgMuted" Color="#6E6E96"/>
        <SolidColorBrush x:Key="FgSubtle" Color="#35355A"/>
        <SolidColorBrush x:Key="BorderCol" Color="#2A2A45"/>

        <conv:PosterImageConverter x:Key="PosterConverter"/>
        <conv:HallTypeBadgeColorConverter x:Key="HallBadgeColor"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="3" 
                          Opacity="0.55" Color="#000000" Direction="270"/>
        <DropShadowEffect x:Key="PosterShadow" BlurRadius="26" ShadowDepth="7"
                          Opacity="0.75" Color="#000000" Direction="270"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="Page">
            <Setter Property="Background" Value="{StaticResource BgDeep}"/>
        </Style>

        <Style x:Key="BackBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="T"
                                   Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}"
                                   FontFamily="{TemplateBinding FontFamily}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="T" Property="Foreground"
                                        Value="{StaticResource FgPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CleanLB" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style x:Key="IndeterminateBar" TargetType="ProgressBar">
            <Setter Property="IsIndeterminate" Value="True"/>
            <Setter Property="Height" Value="4"/>
            <Setter Property="Width" Value="240"/>
            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
            <Setter Property="Background" Value="{StaticResource FgSubtle}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="BusyOverlay" TargetType="Border">
            <Setter Property="Background" Value="#B3000000"/>
            <Setter Property="IsHitTestVisible" Value="True"/>
        </Style>

        <Style x:Key="ErrorBanner" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
            <Setter Property="BorderThickness" Value="3 0 0 0"/>
            <Setter Property="CornerRadius" Value="0 8 8 0"/>
            <Setter Property="Padding" Value="16 12"/>
            <Setter Property="Margin" Value="24 0 24 24"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="MaxWidth" Value="520"/>
        </Style>

        <Style x:Key="CleanLBI" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin"  Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
